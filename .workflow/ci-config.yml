# Durable domain CI configuration — seeded by Vibe Data Studio.
# The VD_DOMAIN_* coordinate keys are Studio-owned and CONVERGE on every spawn;
# the per-platform GHA Variable/Secret name-indirection keys (ADR-0018) are
# defaulted by Studio while custom owner mappings are preserved (ADR-0019).
VD_DOMAIN_DATA_PLATFORM: duckdb_local
VD_DOMAIN_SLUG: uk-nyctaxi-comp
VD_DOMAIN_SCHEMA: main
VD_DOMAIN_DUCKDB_LOCAL_PATH: /Users/ukakkad/vibeData/vd-studio/data/duckdb/uk-nyctaxi-comp.duckdb
VD_DOMAIN_DUCKDB_LOCAL_DATABASE: uk-nyctaxi-comp
